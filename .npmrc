# Ensure consistent package-lock.json
package-lock=true

# Disable optional dependencies by default in CI
optional=false

# Use npm ci for faster CI installs
# progress=false

# Save exact versions (not semver ranges)
save-exact=true